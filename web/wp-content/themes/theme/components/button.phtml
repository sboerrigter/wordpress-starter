<a class="group <?= $class ?? ''; ?>"
    <?php
    if (!empty($url)) {
      echo 'href="' . $url . '" ';
    }
    if (!empty($target)) {
      echo 'target="' . $target . '" ';
    }

    if (!empty($target) && $target === '_blank') {
      echo 'rel="nofollow" ';
    }
   ; ?>
>
  <?= $iconBefore ?? null; ?>
  <?= $title; ?>
  <?= $iconAfter ?? null; ?>
</a>
